# Mesa's rusticl OpenCL driver is compiled in but off by default; it only
# enables the drivers named here. freedreno is the Adreno 619 (reported as
# FD619). Without this, clinfo finds no platforms and hashcat reports
# "No devices found/left", though the GPU itself is fully working.
RUSTICL_ENABLE=freedreno
