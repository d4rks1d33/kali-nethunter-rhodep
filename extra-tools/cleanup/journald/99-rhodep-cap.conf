# rhodep: cap the persistent systemd journal. It had grown to 633M; there is no
# reason to keep that much history on a phone. journald enforces this itself, so
# the journal never gets large again even between cleanup runs.
[Journal]
SystemMaxUse=100M
SystemMaxFileSize=20M
